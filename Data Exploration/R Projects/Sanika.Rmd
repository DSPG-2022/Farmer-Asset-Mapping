---
title: "GDD"
output: html_document
date: '2022-06-23'
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(dplyr)
library(tidyverse)
library(tidycensus)
library(dplyr)
library(ggplot2)
```

```{r}
mydata <- read.csv("GDD_2020.csv")
View(mydata)

newdata <- read.csv("Crop-Info_Farmer Asset Mapping.csv")
View(newdata)
```

```{r}
new_data <- newdata[-c(20:38), ] 
View(new_data)

new_data <- subset(new_data, select = -c(`Type.of.Irrigation`,`Precipitation`, `X`, `X.1`))
  
mydata$Average_temp = (mydata$daily_high_f + mydata$daily_low_f)/2

mydata = subset(mydata, select = -c(`X12z_low_f`, `climate_daily_low_f`, `daily_precip_in`, `X12z_precip_in`, `climate_daily_precip_in`, `X`, `X12z_high_f`, `climate_daily_high_f`, `X.1`, `mrms_precip_in`, `prism_precip_in` ) )

View(mydata)

da

sprintf("%s has a base temp of %.f", new_data$ï..Types.of.Crops , new_data$Base.Temp.in.F)




```
